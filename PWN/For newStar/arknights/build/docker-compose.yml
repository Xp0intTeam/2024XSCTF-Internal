version: '3.8'

services:
    pwn_arknights:
        build: ./
        image: pwn_arknights
        container_name: pwn_arknights
        ports:
            - "8888:8888"
        restart: unless-stopped
