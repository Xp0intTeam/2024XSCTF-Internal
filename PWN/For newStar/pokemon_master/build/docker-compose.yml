version: '3.8'

services:
    pwn_pokemon_master:
        build: ./
        image: pwn_pokemon_master
        container_name: pwn_pokemon_master
        ports:
            - "8888:8888"
        restart: unless-stopped
