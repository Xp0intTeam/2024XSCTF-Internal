version: '3.8'

services:
    pwn_xxxx:
        build: ./
        image: pwn_c_master_plus
        container_name: pwn_c_master_plus
        ports:
            - "8888:8888"
        restart: unless-stopped
